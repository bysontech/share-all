# Copy to .dev.vars for local wrangler dev (not committed).
# R2 S3 API credentials: Cloudflare dashboard → R2 → Manage R2 API Tokens
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME is also in wrangler.toml [vars]; override here if needed.
